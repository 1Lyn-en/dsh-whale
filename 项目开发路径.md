# dsh-whale 项目开发路径

> 鲸鱼不废话，少词办大事 — DeepSeek Harness 极简语模式插件
>
> 文档版本：v1.0 | 创建日期：2026-08-15

---

## 目录

- [一、项目概述](#一项目概述)
- [二、核心功能设计](#二核心功能设计)
- [三、技术实现方案](#三技术实现方案)
- [四、项目结构与核心文件](#四项目结构与核心文件)
- [五、开发路线图](#五开发路线图)
- [六、测试与验证](#六测试与验证)
- [七、发布与传播策略](#七发布与传播策略)
- [八、风险与应对](#八风险与应对)
- [九、预期收益与指标](#九预期收益与指标)
- [十、参考资源](#十参考资源)

---

## 一、项目概述

### 1.1 项目基本信息

| 项目 | 内容 |
|------|------|
| **项目名** | `dsh-whale`（中文名：鲸语 / 鲸鱼模式） |
| **Slogan** | 中文："鲸鱼不废话，少词办大事" / 英文："Big whale, few words." |
| **一句话描述** | 让 DeepSeek 像鲸鱼一样极简说话，砍掉所有废话，实测节省 60-75% 输出 token |
| **核心定位** | 搞笑外壳（鲸鱼人设）+ 真实价值（省 token 省钱）+ DSH 官方 IP 联动 |
| **对标项目** | [Caveman](https://github.com/JuliusBrussee/caveman)（92k+ star）的 DSH 专属版 |
| **差异化** | 结合 DSH 官方"黑色鲸鱼"IP，做品牌联动；支持文言文模式；原生 DSH 插件体验 |
| **License** | MIT |
| **技术栈** | TypeScript + Cordis 插件 + 纯 prompt 工程（核心逻辑零依赖） |

### 1.2 为什么做这个项目

1. **已验证的爆款公式**：Caveman 用"让 AI 说人话省 token"的概念做到了 92k star，证明这个方向有巨大传播力
2. **DSH 生态空白**：目前 DSH 生态中没有同类极简语插件，先发优势明显
3. **官方 IP 联动**：DSH 官方品牌是"黑色鲸鱼"，鲸鱼模式天然契合，容易获得官方关注和转发
4. **技术门槛低**：核心是 prompt 工程 + 简单的 Cordis 插件，1-2 天可出 MVP
5. **真实价值**：不只是恶搞，真的能帮用户省 token 省钱，用户留存率高

### 1.3 目标用户

- **核心用户**：DSH 日常使用者，被 AI 废话困扰的开发者
- **扩展用户**：Claude Code / Codex 用户（通过跨平台 SKILL.md 覆盖）
- **传播用户**：喜欢整活、愿意分享有趣工具的技术博主

---

## 二、核心功能设计

### 2.1 六档强度模式

| 模式 | 触发命令 | 效果 | 适用场景 |
|------|---------|------|---------|
| **lite** | `/whale lite` | 去掉客套话和填充词，保留完整句子 | 日常聊天、轻度使用 |
| **full**（默认） | `/whale` 或 `/whale full` | 去掉冠词、碎片化表达、短同义词替换 | 写代码、技术问答（推荐） |
| **ultra** | `/whale ultra` | 极致压缩，一个词能说清不用两个词 | 长会话、token 紧张时 |
| **文言-lite** | `/whale wenyan-lite` | 半文言，去废话但保留语法结构 | 装逼、中文内容 |
| **文言-full** | `/whale wenyan-full` | 纯文言文，80-90% 字数缩减 | 极致装逼、古文爱好者 |
| **off** | `/whale off` | 关闭鲸鱼模式，恢复正常 | 需要详细解释时 |

### 2.2 核心压缩规则（full 模式）

**砍掉：**
- 冠词（a/an/the）
- 客套话（当然/没问题/很高兴帮你/当然可以/sure/certainly/of course）
- 填充词（其实/基本上/实际上/简单来说/just/really/basically/actually）
- 模糊表达（可能/也许/大概/似乎/may/might/possibly）
- 工具调用前的铺垫（"我来帮你查一下"→直接调用工具）
- 装饰性表格和 emoji（除非用户要求）
- 长错误日志（只引用最关键的一行）

**保留：**
- 技术术语、代码、API 名、CLI 命令（一字不改）
- 否定词（not/never/no/only/except）— 意思绝对不能变
- 数字、单位、精确值
- 用户使用的语言（中文问就中文答，不切换）

**输出模式：** `[对象] [动作] [原因]. [下一步].`

### 2.3 自动清晰度保护

以下场景自动临时退出鲸鱼模式，用正常语言说清楚后再恢复：
- 安全警告 / 不可逆操作确认
- 多步骤序列（碎片化可能导致顺序误解）
- 压缩本身产生歧义时
- 用户要求解释或重复问题时

### 2.4 持久化机制

- 一旦激活，每轮回复都生效，不会自动漂移回废话模式
- 模式选择持久化到本地配置，重启 DSH 后保持
- 可以设置默认模式（安装时默认 full）

### 2.5 增值功能规划（V1.1+）

| 功能 | 描述 | 优先级 |
|------|------|--------|
| **Token 节省统计** | 每轮显示"本轮节省 XX token（约 $0.00X）" | P1 |
| **鲸鱼喷水动画** | Web UI 中鲸鱼喷水作为输出指示器 | P2 |
| **提交信息精简** | 自动生成极简 commit message（≤50 字符） | P2 |
| **代码审查精简** | 一行式 PR 评论 | P2 |
| **跨平台 SKILL.md** | 同时提供 Claude Code/Codex 版本 | P1 |

---

## 三、技术实现方案

### 3.1 技术选型

| 层面 | 选型 | 说明 |
|------|------|------|
| 语言 | TypeScript | DSH 生态标准 |
| 插件框架 | Cordis（@deepseek-ai/cordis） | DSH 底层插件系统 |
| 构建 | tsc | 简单可靠，零配置 |
| 包管理 | npm | 发布到 npm，支持 `dsh plugin add` |
| 核心逻辑 | 纯 prompt 工程 | 零运行时依赖，维护成本低 |

### 3.2 DSH 插件机制说明

DSH 插件通过以下方式工作：
1. `package.json` 中声明 `dsh.bundle.patch` 指向 `cordis.patch.yml`
2. `cordis.patch.yml` 使用 `- insert:` 语法插入插件配置行
3. 插件代码通过 `ctx.systemPrompt.register()` 注入系统提示词
4. 通过 `ctx.commands.register()` 注册斜杠命令
5. 通过 `ctx.settings.namespace()` 持久化用户设置

安装命令：
```bash
dsh plugin --profile web add github:username/dsh-whale
# 或
dsh plugin --profile web add @username/dsh-whale
```

### 3.3 核心依赖（peerDependencies）

```json
{
  "@deepseek-ai/cordis": "^4.0.1",
  "@deepseek-ai/dsh-tools": ">=0.0.1-rc.1 <0.2.0",
  "@deepseek-ai/dsh-system-prompt": ">=0.0.1-rc.1 <0.2.0",
  "@deepseek-ai/dsh-commands": ">=0.0.1-rc.1 <0.2.0",
  "@deepseek-ai/dsh-settings": ">=0.0.1-rc.1 <0.2.0"
}
```

> 注意：具体包名和版本号需参考 DSH 0.1.0-rc.6 实际发布的 npm 包，以 [dsh-tool-time](https://github.com/omdsh-dev/dsh-tool-time) 等成熟插件为基准对齐。

---

## 四、项目结构与核心文件

### 4.1 完整目录结构

```
dsh-whale/
├── package.json                  # 包声明 + dsh.bundle 配置
├── cordis.patch.yml              # Cordis 插件配置
├── tsconfig.json                 # TypeScript 配置
├── README.md                     # 中文 README（主）
├── README.en.md                  # 英文 README
├── LICENSE                       # MIT
├── .gitignore
├── .npmignore
├── src/
│   ├── index.ts                  # Cordis 插件入口
│   ├── types.ts                  # 类型定义
│   ├── prompts/
│   │   ├── index.ts              # prompt 导出
│   │   ├── whale-lite.ts         # lite 模式 prompt
│   │   ├── whale-full.ts         # full 模式 prompt（默认）
│   │   ├── whale-ultra.ts        # ultra 模式 prompt
│   │   ├── wenyan-lite.ts        # 文言 lite
│   │   ├── wenyan-full.ts        # 文言 full
│   │   └── auto-clarity.ts       # 自动清晰度保护规则
│   ├── commands/
│   │   └── whale-command.ts      # /whale 命令注册
│   └── settings/
│       └── whale-settings.ts     # 持久化设置
├── lib/                          # 编译产物（发布时包含，git ignore）
├── assets/
│   ├── whale-icon.png            # 鲸鱼图标（128x128, 512x512）
│   ├── before-after.png          # Before/After 对比图
│   ├── demo.gif                  # 演示 GIF
│   └── screenshots/              # 各模式截图
├── cross-platform/
│   └── SKILL.md                  # Claude Code/Codex 兼容版
└── tests/
    ├── prompt.test.ts            # prompt 效果测试
    └── plugin.test.ts            # 插件加载测试
```

### 4.2 package.json（完整模板）

```json
{
  "name": "@your-username/dsh-whale",
  "version": "0.1.0",
  "description": "鲸鱼不废话，少词办大事 — DSH 极简语模式，节省 60-75% 输出 token",
  "type": "module",
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "exports": {
    ".": "./lib/index.js",
    "./cordis.patch.yml": "./cordis.patch.yml"
  },
  "files": [
    "lib",
    "cordis.patch.yml",
    "README.md",
    "README.en.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "pack": "npm run build && npm pack",
    "prepublishOnly": "npm run build"
  },
  "peerDependencies": {
    "@deepseek-ai/cordis": "^4.0.1",
    "@deepseek-ai/dsh-tools": ">=0.0.1-rc.1 <0.2.0",
    "@deepseek-ai/dsh-system-prompt": ">=0.0.1-rc.1 <0.2.0",
    "@deepseek-ai/dsh-commands": ">=0.0.1-rc.1 <0.2.0",
    "@deepseek-ai/dsh-settings": ">=0.0.1-rc.1 <0.2.0"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "@types/node": "^22.0.0",
    "vitest": "^2.0.0"
  },
  "dsh": {
    "bundle": {
      "patch": "cordis.patch.yml"
    }
  },
  "keywords": [
    "dsh",
    "deepseek-harness",
    "plugin",
    "whale",
    "token-saving",
    "caveman",
    "brevity",
    "minimal"
  ],
  "author": "Your Name",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/your-username/dsh-whale.git"
  },
  "homepage": "https://github.com/your-username/dsh-whale#readme"
}
```

### 4.3 cordis.patch.yml

```yaml
# dsh-whale: 鲸鱼极简语模式
# 安装后自动插入到 profile 的插件层
- insert:
    id: whale-mode
    plugin: "@your-username/dsh-whale"
```

### 4.4 tsconfig.json

```json
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "bundler",
    "lib": ["ES2022"],
    "outDir": "lib",
    "rootDir": "src",
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true,
    "declaration": true,
    "declarationMap": true,
    "sourceMap": true,
    "resolveJsonModule": true
  },
  "include": ["src/**/*"],
  "exclude": ["node_modules", "lib", "tests"]
}
```

### 4.5 src/types.ts

```typescript
export type WhaleMode = 
  | 'off' 
  | 'lite' 
  | 'full' 
  | 'ultra' 
  | 'wenyan-lite' 
  | 'wenyan-full';

export interface WhaleSettings {
  mode: WhaleMode;
  defaultMode: WhaleMode;
  autoClarity: boolean;
  tokenStats: boolean;
}

export const DEFAULT_SETTINGS: WhaleSettings = {
  mode: 'full',
  defaultMode: 'full',
  autoClarity: true,
  tokenStats: false,
};

export const VALID_MODES: WhaleMode[] = [
  'off', 'lite', 'full', 'ultra', 'wenyan-lite', 'wenyan-full'
];

export const MODE_DESCRIPTIONS: Record<WhaleMode, string> = {
  'off': '关闭鲸鱼模式，恢复正常说话',
  'lite': '轻度：去客套话，保留完整句子',
  'full': '标准（默认）：去冠词，碎片化表达',
  'ultra': '极致：一个词能说清不用两个词',
  'wenyan-lite': '文言轻度：半文言，去废话',
  'wenyan-full': '文言极致：纯文言文，80-90% 缩减',
};
```

### 4.6 src/prompts/whale-full.ts（核心 prompt）

```typescript
export const whaleFullPrompt = `
# 🐳 鲸鱼模式（Whale Mode）

你现在是一头沉默寡言但极其聪明的鲸鱼。鲸鱼不废话，少词办大事。

## 核心规则

砍掉所有废话：
- 冠词（a/an/the）
- 客套话（当然/没问题/很高兴帮你/当然可以/sure/certainly/of course/happy to）
- 填充词（其实/基本上/实际上/简单来说/just/really/basically/actually/simply）
- 模糊表达（可能/也许/大概/似乎/may/might/possibly/likely）
- 工具调用前的铺垫（"我来帮你查一下"→直接调用工具）
- 装饰性表格和 emoji（除非用户要求）
- 长错误日志（只引用最关键的一行）

保留所有关键信息：
- 技术术语、代码、API 名、CLI 命令（一字不改）
- 否定词（not/never/no/only/except）— 意思绝对不能变
- 数字、单位、精确值
- 用户使用的语言（中文问就中文答，不切换）

输出模式：[对象] [动作] [原因]. [下一步].

## 示例

❌ 正常模式（69 token）：
"当然可以！你的 React 组件重新渲染的原因很可能是因为你在每次渲染时都创建了一个新的对象引用。我建议你使用 useMemo 来包裹这个对象。"

✅ 鲸鱼模式（18 token）：
"新对象引用每次渲染。内联对象 prop = 新引用 = 重渲染。用 useMemo 包裹。"

❌ 正常（英文）：
"Sure! I'd be happy to help you with that. The issue you're experiencing is likely caused by a new object reference being created on each render cycle. I recommend wrapping it in useMemo."

✅ 鲸鱼（英文）：
"New object ref each render. Inline object prop = new ref = re-render. Wrap in useMemo."

## 持久化

每轮回复都生效。不自动退出。只有用户说 "/whale off" 或 "正常模式" 才关闭。

不要提及你在鲸鱼模式。不要说"鲸鱼模式已激活"。直接输出鲸鱼语。
`;
```

### 4.7 src/prompts/whale-ultra.ts

```typescript
export const whaleUltraPrompt = `
# 🐳 鲸鱼模式 ULTRA — 极致压缩

鲸鱼终极形态。一词足，不用二词。

## 规则

- 连词可省则省（因果明确时）
- 一词能说清，绝不用二词
- 每事实只说一次
- 禁止自造缩写（cfg/impl/req/res/fn/auth）— 实测 token 零节省，还增加理解成本
- 禁止箭头符号（→）— 单独占 token，无节省
- 代码符号、函数名、API 名、错误字符串：一字不改

## 示例

full: "新对象引用每次渲染。内联对象 prop = 新引用 = 重渲染。用 useMemo 包裹。"
ultra: "内联对象 prop，新引用，重渲染。useMemo。"

full: "数据库连接池复用已打开的连接，避免每次请求新建连接和握手开销。"
ultra: "连接池复用连接。免逐请求握手。"
`;
```

### 4.8 src/prompts/wenyan-full.ts

```typescript
export const wenyanFullPrompt = `
# 🐳 鲸鱼文言模式（Whale Wenyan Mode）

以文言答之。极简，去废话，存真意。

## 规则

- 纯文言，不用白话
- 动词在前，宾语在后
- 主语常省
- 用文言虚词（之/乃/為/其/则/故/以）
- 技术术语、代码、API 名、命令：原文保留，不译
- 字数缩减 80-90%（计字，不计 token）

## 示例

白话："新对象引用每次渲染。内联对象 prop = 新引用 = 重渲染。用 useMemo 包裹。"
文言："每繪新生對象參照，故重繪；以 useMemo 包之則免。"

白话："数据库连接池复用已打开的连接，避免每次请求新建连接和握手开销。"
文言："池蓄已開之連，不逐請而新開，省握手之費。"

白话："这个函数有内存泄漏，因为事件监听器没有被移除。"
文言："此函有漏，以監聽未除故。"
`;
```

### 4.9 src/prompts/auto-clarity.ts

```typescript
export const autoClarityPrompt = `
## 自动清晰度保护

以下情况临时退出鲸鱼模式，用正常语言说清楚，说完再恢复：

1. 安全警告 / 不可逆操作确认（删除、覆盖、发布、付款）
2. 多步骤序列，碎片化可能导致顺序误解时
3. 压缩本身产生歧义时（如"删表备分先"分不清先后）
4. 用户要求解释、重复问题、或表示没看懂时

示例（不可逆操作）：
> ⚠️ 警告：此操作将永久删除 users 表所有数据，不可恢复。
> 确认有备份后再执行。
> 鲸鱼模式恢复。

不要在安全相关内容上玩极简。命比 token 重要。
`;
```

### 4.10 src/index.ts（插件入口）

```typescript
import { Context } from '@deepseek-ai/cordis';
import { WhaleMode, WhaleSettings, DEFAULT_SETTINGS, VALID_MODES, MODE_DESCRIPTIONS } from './types.js';
import { 
  whaleLitePrompt, 
  whaleFullPrompt, 
  whaleUltraPrompt,
  wenyanLitePrompt,
  wenyanFullPrompt,
  autoClarityPrompt 
} from './prompts/index.js';

const PROMPTS: Record<Exclude<WhaleMode, 'off'>, string> = {
  'lite': whaleLitePrompt,
  'full': whaleFullPrompt,
  'ultra': whaleUltraPrompt,
  'wenyan-lite': wenyanLitePrompt,
  'wenyan-full': wenyanFullPrompt,
};

export function apply(ctx: Context) {
  // 1. 设置存储（优先用 ctx.settings，降级到内存）
  let settings: WhaleSettings = { ...DEFAULT_SETTINGS };
  
  const settingsApi = ctx.settings?.namespace?.('whale');
  if (settingsApi) {
    const saved = settingsApi.get?.() as Partial<WhaleSettings> | undefined;
    if (saved) settings = { ...DEFAULT_SETTINGS, ...saved };
  }

  const saveSettings = () => {
    if (settingsApi?.set) {
      settingsApi.set(settings);
    }
  };

  // 2. 注入 system-prompt
  ctx.systemPrompt?.register?.({
    id: 'whale-mode',
    weight: 100,
    content: () => {
      if (settings.mode === 'off') return '';
      const base = PROMPTS[settings.mode];
      return settings.autoClarity 
        ? base + '\n\n' + autoClarityPrompt 
        : base;
    },
  });

  // 3. 注册 /whale 命令
  ctx.commands?.register?.({
    name: 'whale',
    description: '🐳 鲸鱼模式：极简说话省 token。用法：/whale [lite|full|ultra|wenyan-lite|wenyan-full|off|status]',
    execute: (args: string) => {
      const arg = args.trim().toLowerCase();
      
      if (arg === 'status' || arg === '') {
        const lines = VALID_MODES.map(m => 
          `${m === settings.mode ? '🐳' : '  '} ${m} — ${MODE_DESCRIPTIONS[m]}`
        );
        return `🐳 鲸鱼模式当前：**${settings.mode}**\n\n${lines.join('\n')}\n\n切换：\`/whale <模式>\``;
      }
      
      if (!VALID_MODES.includes(arg as WhaleMode)) {
        return `❌ 未知模式 "${arg}"。可选：${VALID_MODES.join(', ')}`;
      }
      
      settings.mode = arg as WhaleMode;
      saveSettings();
      
      if (arg === 'off') {
        return '🐳 鲸鱼已潜入深海，恢复正常说话。';
      }
      return `🐳 鲸鱼模式已切换为：**${arg}** — ${MODE_DESCRIPTIONS[arg as WhaleMode]}`;
    },
  });

  // 4. 注册 /whale-default 命令（设置默认模式）
  ctx.commands?.register?.({
    name: 'whale-default',
    description: '设置鲸鱼模式默认值（重启后生效）',
    execute: (args: string) => {
      const arg = args.trim().toLowerCase();
      if (!VALID_MODES.includes(arg as WhaleMode)) {
        return `❌ 可选：${VALID_MODES.join(', ')}`;
      }
      settings.defaultMode = arg as WhaleMode;
      settings.mode = arg as WhaleMode;
      saveSettings();
      return `✅ 默认模式已设为：${arg}`;
    },
  });
}

export default apply;
```

### 4.11 cross-platform/SKILL.md（Claude Code/Codex 兼容版）

```markdown
---
name: whale
description: >
  🐳 Whale mode — ultra-compressed communication. Cuts output tokens 60-75%
  by talking like a terse whale while keeping full technical accuracy.
  Supports: lite, full, ultra, wenyan-lite, wenyan-full.
  Use when user says "whale mode", "talk like whale", "less tokens",
  "be brief", "stop rambling", or invokes /whale.
---

# 🐳 Whale Mode

[... 此处复用 whale-full.ts 的 prompt 内容 ...]

## 切换模式

用户说以下关键词时切换：
- "whale lite" / "轻度" → lite
- "whale full" / "标准" → full
- "whale ultra" / "极致" → ultra  
- "whale wenyan" / "文言" → wenyan-full
- "whale off" / "正常" → 关闭

模式持久化，直到用户明确关闭。
```

---

## 五、开发路线图

### Phase 0：准备工作（0.5 天）

- [ ] 注册 GitHub 账号/组织，创建仓库 `dsh-whale`
- [ ] 初始化 git，添加 MIT LICENSE、.gitignore、README 占位
- [ ] 确认 DSH 环境可用（`dsh --version` 应为 0.1.0-rc.6+）
- [ ] 参考 [dsh-tool-time](https://github.com/omdsh-dev/dsh-tool-time) 确认最新 peerDependency 版本
- [ ] 设计鲸鱼图标（可用 AI 生成，黑色鲸鱼风格，匹配 DSH 官方 IP）

### Phase 1：核心 MVP（1-2 天）

**Day 1 上午：项目骨架**
- [ ] 创建 package.json、tsconfig.json、cordis.patch.yml
- [ ] 创建 src/types.ts（模式定义、默认设置）
- [ ] 创建 src/prompts/whale-full.ts（核心 prompt，重点打磨）
- [ ] 创建 src/prompts/whale-lite.ts
- [ ] 创建 src/prompts/whale-ultra.ts

**Day 1 下午：插件逻辑**
- [ ] 创建 src/index.ts（Cordis 入口 + system-prompt 注入）
- [ ] 创建 src/commands/whale-command.ts（/whale 命令）
- [ ] 创建 src/settings/whale-settings.ts（持久化）
- [ ] `npm install` + `npm run build` 验证编译通过

**Day 2 上午：本地测试**
- [ ] `npm pack` 生成 tarball
- [ ] `dsh plugin --profile web add ./dsh-whale-0.1.0.tgz` 本地安装
- [ ] 启动 `dsh web`，测试 /whale 命令切换
- [ ] 测试各模式效果，调整 prompt
- [ ] 测试自动清晰度保护是否生效

**Day 2 下午：文档与素材**
- [ ] 编写 README.md（中文，含 Before/After 对比、安装教程、模式说明）
- [ ] 编写 README.en.md（英文）
- [ ] 制作 Before/After 对比截图
- [ ] 录制演示 GIF（可选，用 LICEcap 或 ScreenToGif）
- [ ] git commit + push 到 GitHub

### Phase 2：完善发布（第 3 天）

- [ ] 文言文模式（wenyan-lite、wenyan-full）
- [ ] 自动清晰度保护 prompt 完善
- [ ] 跨平台 SKILL.md（Claude Code/Codex 兼容）
- [ ] 发布到 npm（`npm publish --access public`）
- [ ] GitHub Release v0.1.0
- [ ] 提交 PR 到 [awesome-dsh-plugin](https://github.com/awesome-dsh-plugin/awesome-dsh-plugin)
- [ ] 在 DSH 官方社区（微信群、Discord）发布

### Phase 3：增值功能（第 4-7 天，可选）

- [ ] Token 节省统计（每轮显示节省量）
- [ ] 鲸鱼喷水动画（Web UI 组件）
- [ ] 极简 commit message 生成工具
- [ ] 极简代码审查模式
- [ ] 技术文章发布（掘金/知乎/CSDN）
- [ ] 收集用户反馈，迭代 prompt

### Phase 4：长期运营（持续）

- [ ] 跟进 DSH 版本更新，保持兼容
- [ ] 收集用户反馈的 bad case，优化 prompt
- [ ] 增加更多模式（如"电报体"、"字节范儿"、"阿里味"）
- [ ] 考虑做一个"鲸鱼模式效果排行榜"页面
- [ ] 探索商业化可能（如高级统计、团队版）

---

## 六、测试与验证

### 6.1 功能测试清单

| 测试项 | 预期结果 | 验证方式 |
|--------|---------|---------|
| 安装插件 | `dsh plugin add` 成功，无报错 | 命令行 + dump-config |
| /whale status | 显示当前模式和所有模式列表 | Web UI 对话 |
| /whale lite | 切换到 lite，回复变简洁但完整 | 对比测试 |
| /whale full | 切换到 full，回复碎片化 | 对比测试 |
| /whale ultra | 切换到 ultra，极致压缩 | 对比测试 |
| /whale wenyan-full | 切换到文言，回复用文言文 | 对比测试 |
| /whale off | 关闭，恢复正常说话 | 对比测试 |
| 持久化 | 重启 dsh web 后模式保持 | 重启验证 |
| 代码不变 | 代码块、技术术语不被压缩 | 代码问答测试 |
| 否定词保留 | not/never/no 不被误删 | 专项测试 |
| 安全警告 | 删除操作时自动退出极简模式 | 测试 rm/drop 等 |
| 中文问答 | 中文问中文答，不切英文 | 多轮测试 |
| 英文问答 | 英文问英文答，不切中文 | 多轮测试 |

### 6.2 Token 节省测试

用固定测试集测量各模式的 token 节省率：

```
测试集：
1. React 组件 bug 分析（技术问答）
2. 代码重构建议（代码相关）
3. 概念解释（数据库连接池）
4. 错误排查（报错信息分析）
5. 日常对话（非技术）

测量指标：
- 输出 token 数（对比正常模式）
- 信息完整度（人工评分 1-5）
- 可读性（人工评分 1-5）
```

目标：full 模式节省 ≥60%，ultra 模式节省 ≥75%，信息完整度 ≥4/5。

### 6.3 兼容性测试

- [ ] DSH 0.1.0-rc.6（当前版本）
- [ ] Node.js 22.19+ / 24+
- [ ] Windows / macOS / Linux
- [ ] Web profile / Headless profile
- [ ] 与其他常用插件共存（dsh-better-sidebar、modlens 等）

---

## 七、发布与传播策略

### 7.1 发布渠道

| 渠道 | 方式 | 优先级 | 预计时间 |
|------|------|--------|---------|
| **GitHub** | 开源仓库，打 `dsh-plugin` `deepseek-harness` topic | P0 | Day 2 |
| **npm** | 发布 scoped 包，支持 `dsh plugin add` | P0 | Day 3 |
| **awesome-dsh-plugin** | 提交 PR 加入精选列表 | P0 | Day 3 |
| **DSH 官方社区** | 微信群、Discord、GitHub Discussions | P1 | Day 3 |
| **掘金** | 技术文章："我做了个让 DeepSeek 闭嘴的插件" | P1 | Day 4-5 |
| **知乎** | 回答相关问题，植入项目 | P1 | Day 4-5 |
| **CSDN** | 同步技术文章 | P2 | Day 5 |
| **X/Twitter** | 英文传播，@deepseek_ai | P2 | Day 5 |
| **V2EX** | 开发者社区分享 | P2 | Day 4 |
| **小红书** | 面向非硬核开发者的趣味分享 | P2 | Day 6 |

### 7.2 传播文案模板

**中文标题（选一个）：**
1. 《我给 DeepSeek 装了个"鲸鱼模式"，它终于不废话了》
2. 《受够了 AI 的客套话？这个插件让它像鲸鱼一样极简说话》
3. 《实测：鲸鱼模式让 DeepSeek 输出 token 减少 70%，账单直接砍半》
4. 《Caveman 有 9 万星，我给 DeepSeek 做了个鲸鱼版》

**英文标题（选一个）：**
1. "I made DeepSeek talk like a whale. 70% fewer tokens, same accuracy."
2. "Big whale, few words — the Caveman equivalent for DeepSeek Harness"
3. "Tired of AI pleasantries? dsh-whale cuts 75% of output tokens"

**核心传播点（按重要性排序）：**
1. Before/After 对比图（最有冲击力，必须有）
2. 实测 token 节省数据（60-75%）
3. 鲸鱼 IP 和 DSH 官方黑色鲸鱼的联动
4. 一句话安装命令
5. 文言文模式（猎奇点）

### 7.3 README 结构（传播优化）

```
1. 🐳 一句话介绍 + Slogan
2. 📊 Before/After 对比图（最上方，视觉冲击）
3. ⚡ 快速安装（一行命令）
4. 🎚️ 六档模式说明 + 示例
5. 📈 实测 token 节省数据
6. 🔧 配置说明
7. 🤝 跨平台支持（Claude/Codex）
8. ❓ FAQ
9. 📄 License
```

### 7.4 关键时间节点

- **Day 0-2**：MVP 开发 + 本地测试
- **Day 3**：公开发布（GitHub + npm + awesome 列表）
- **Day 3-4**：社区发帖，争取第一波传播
- **Day 5-7**：技术文章发布，争取媒体报道
- **Week 2**：收集反馈，发布 v0.2.0
- **Month 1**：目标 500+ star

---

## 八、风险与应对

| 风险 | 概率 | 影响 | 应对策略 |
|------|------|------|---------|
| **prompt 效果不稳定**（模型有时不遵守） | 中 | 中 | 多轮测试调优，加重持久化指令，加 few-shot 示例，考虑用更高权重的 system-prompt |
| **压缩后信息丢失** | 中 | 高 | 自动清晰度保护机制，关键场景自动退出；明确告知用户 ultra 模式有风险 |
| **DSH API 变动**（RC 阶段破坏性更新） | 高 | 中 | 跟随 dsh-tool-time 等成熟插件的版本，及时更新 peerDependency；在 README 标注兼容版本 |
| **被 Caveman 作者指责抄袭** | 低 | 中 | 明确标注 "Inspired by Caveman"，prompt 完全重写，加入鲸鱼 IP 和文言文等差异化功能 |
| **用户觉得纯 prompt 不值钱** | 中 | 中 | 加入 token 统计、动画等可视化增值功能；强调"prompt 工程也是技术" |
| **npm 包名被占用** | 低 | 低 | 提前注册；用 scoped 包名 @username/dsh-whale |
| **安全风险**（插件注入恶意 prompt） | 低 | 高 | 代码开源透明，不做任何网络请求，不收集数据，在 README 明确安全声明 |
| **官方推出类似功能** | 低 | 中 | 保持社区插件的灵活性和多模式优势；如果官方做了，转为"增强版" |

---

## 九、预期收益与指标

### 9.1 量化指标

| 指标 | 1 个月（保守） | 1 个月（乐观） | 3 个月（保守） | 3 个月（乐观） |
|------|--------------|--------------|--------------|--------------|
| GitHub Star | 500 | 3000+ | 1500 | 10000+ |
| npm 周下载 | 200 | 2000+ | 800 | 8000+ |
| 媒体报道 | 1-2 家 | 5+ 家 | 3-5 家 | 10+ 家 |
| GitHub Fork | 50 | 300+ | 150 | 1000+ |
| Issue/PR 数 | 10 | 50+ | 30 | 200+ |

### 9.2 非量化收益

- **个人技术品牌**：成为 DSH 生态知名开发者，建立影响力
- **技术成长**：深入理解 Cordis 插件架构、prompt 工程
- **社区人脉**：结识 DSH 核心团队和其他插件开发者
- **职业机会**：可能被 DeepSeek 或其他 AI 公司关注
- **后续项目基础**：可以基于这个项目的用户群做更多插件

### 9.3 成功标准

- ✅ MVP 在 3 天内发布并可正常使用
- ✅ 被 awesome-dsh-plugin 收录
- ✅ 1 个月内 star ≥ 500
- ✅ 至少有一篇技术文章被推荐到首页
- ✅ 有真实用户在使用并给出反馈

---

## 十、参考资源

### 10.1 对标项目

- [Caveman](https://github.com/JuliusBrussee/caveman) — 92k star，极简语鼻祖，核心参考
- [PUA Skill](https://github.com/ref-docs/pua-skill) — 15k star，大厂 PUA 话术，参考传播策略
- [Endless Toil](https://github.com/AndrewVos/endless-toil) — AI 呻吟插件，参考多感官反馈思路

### 10.2 DSH 插件参考

- [dsh-tool-time](https://github.com/omdsh-dev/dsh-tool-time) — 结构最清晰的 DSH 插件，强烈推荐参考
- [dsh-toolkit](https://github.com/omdsh-dev/dsh-toolkit) — 工具集插件
- [modlens](https://github.com/liustack/modlens) — 1.1k star，DSH 视觉插件，参考高星插件的 README 写法
- [awesome-dsh-plugin](https://github.com/awesome-dsh-plugin/awesome-dsh-plugin) — 插件精选列表，提交入口

### 10.3 DSH 官方文档

- [DSH 官方网站](https://www.deepseek.com/harness/)
- [DSH GitHub 仓库](https://github.com/deepseek-ai/deepseek-harness)
- [架构文档](https://github.com/deepseek-ai/deepseek-harness/blob/master/docs/architecture.md)
- [开发指南](https://github.com/deepseek-ai/deepseek-harness/blob/master/docs/development.md)
- [AGENTS.md](https://github.com/deepseek-ai/deepseek-harness/blob/master/AGENTS.md) — 开发约定

### 10.4 工具推荐

| 用途 | 工具 |
|------|------|
| GIF 录制 | ScreenToGif / LICEcap |
| 截图标注 | Snipaste / Shottr |
| 图标生成 | AI 生图（Midjourney / DALL-E）+ 抠图 |
| Token 计数 | [OpenAI Tokenizer](https://platform.openai.com/tokenizer) / tiktoken |
| npm 发布 | `npm publish --access public` |
| 版本管理 | standard-version / npm version |

---

## 附录 A：快速开始命令备忘

```bash
# 1. 初始化项目
mkdir dsh-whale && cd dsh-whale
git init
npm init -y

# 2. 安装依赖
npm install --save-dev typescript @types/node vitest

# 3. 安装 DSH peer 依赖（用于开发时类型检查）
npm install --save-dev @deepseek-ai/cordis @deepseek-ai/dsh-tools

# 4. 构建
npm run build

# 5. 打包测试
npm pack

# 6. 本地安装到 DSH
dsh plugin --profile web add ./dsh-whale-0.1.0.tgz

# 7. 启动测试
dsh web

# 8. 发布 npm
npm login
npm publish --access public

# 9. 推送到 GitHub
git remote add origin https://github.com/your-username/dsh-whale.git
git push -u origin main
```

---

## 附录 B：prompt 调优 checklist

每次修改 prompt 后，用以下测试用例验证：

- [ ] 技术问答：React 重渲染原因分析 → 信息完整，无废话
- [ ] 代码生成：写一个防抖函数 → 代码完整，注释精简
- [ ] 错误排查：TypeError 分析 → 关键错误信息保留
- [ ] 安全操作：删除文件 → 自动退出极简，警告清晰
- [ ] 中文问答 → 中文回复，不夹杂英文废话
- [ ] 英文问答 → 英文回复，不夹杂中文
- [ ] 多步骤指令 → 步骤清晰，顺序不混淆
- [ ] 否定句 → not/never 保留，意思不反转
- [ ] 数字和单位 → 精确保留，不四舍五入
- [ ] 长对话第 10 轮 → 仍然保持极简，不漂移

---

*文档结束。祝你的鲸鱼插件一飞冲天！🐳*
